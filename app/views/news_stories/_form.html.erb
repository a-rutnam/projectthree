<%= form_with(model: news_story, local: true) do |form| %>
  <% if news_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_story.errors.count, "error") %> prohibited this news_story from being saved:</h2>

      <ul>
      <% news_story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :news_story_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
